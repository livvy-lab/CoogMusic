
{/*
route for listener
app.get('/listener', async (req, res) => {
  try {
    const [rows] = await pool.query('SELECT * FROM listener');
  }catch (err) {  
    console.error(err);
    res.status(500).json({ error: 'failed to get listener' });
  }
});
*/}